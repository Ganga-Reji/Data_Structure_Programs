#include<stdio.h>
Struct node
{
	int data;
	struct node*next;
};

Struct node*head,*temp,*newnode;
head=0;
//creating a newnode
//linkeedlist has two parts :data,pointer
int choice;
while(choice==1)
{
newnode=(struct node*)malloc(sizeof(structnode))
printf("enter data");
scanf("%d",newnode->data);//data of newnode assigned
newnode->next=0;//pointer assigned	

//putting this node in linked list
if(head==null)
{
	head==newnode;
	head
}	
else
{


